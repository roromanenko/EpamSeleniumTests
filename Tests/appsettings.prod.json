{
  "_comment": "Per-environment overlay applied on top of appsettings.json when TEST_ENV=prod. Logging restricted to warnings and errors for production runs.",
  "BaseUrl": "https://www.epam.com",
  "Logging": {
    "MinLevel": "Warn"
  }
}
